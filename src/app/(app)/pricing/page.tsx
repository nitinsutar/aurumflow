import { Section, StatCard } from "@/components/ui";
import { calculatePricing } from "@/lib/calculations";

export default function PricingPage() {
  const result = calculatePricing({ netMetalWeight: 42, currentMetalRate: 5960, stoneCost: 48000, makingCharge: 42000, wastagePercentage: 3, marginPercentage: 12, gstPercentage: 3 });
  return <div className="space-y-6"><h1 className="text-2xl font-semibold text-ink">Jewellery Pricing Engine</h1><div className="grid gap-6 xl:grid-cols-[.9fr_1.1fr]"><Section title="Calculator Inputs"><form className="grid gap-3"><div className="grid gap-3 sm:grid-cols-2"><select><option>Gold</option><option>Silver</option><option>Platinum</option></select><select><option>22K</option><option>24K</option><option>18K</option><option>Custom purity</option></select><input placeholder="Net metal weight" defaultValue="42" /><input placeholder="Current metal rate" defaultValue="5960" /><input placeholder="Stone cost" defaultValue="48000" /><input placeholder="Labour / making charge" defaultValue="42000" /><input placeholder="Wastage %" defaultValue="3" /><input placeholder="Margin %" defaultValue="12" /><input placeholder="GST %" defaultValue="3" /></div><button className="rounded-md bg-ink px-4 py-2 text-sm font-semibold text-white">Calculate and save quote</button></form></Section><div className="grid gap-4 sm:grid-cols-2"><StatCard label="Metal value" value={`Rs ${Math.round(result.metalValue).toLocaleString("en-IN")}`} /><StatCard label="Wastage value" value={`Rs ${Math.round(result.wastageValue).toLocaleString("en-IN")}`} /><StatCard label="Making charge" value={`Rs ${Math.round(result.makingCharge).toLocaleString("en-IN")}`} /><StatCard label="Stone value" value={`Rs ${Math.round(result.stoneValue).toLocaleString("en-IN")}`} /><StatCard label="Subtotal" value={`Rs ${Math.round(result.subtotal).toLocaleString("en-IN")}`} /><StatCard label="GST" value={`Rs ${Math.round(result.gst).toLocaleString("en-IN")}`} /><div className="sm:col-span-2"><StatCard label="Final selling price" value={`Rs ${Math.round(result.finalSellingPrice).toLocaleString("en-IN")}`} tone="gold" /></div></div></div></div>;
}
