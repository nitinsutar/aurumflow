import { Badge, Money, Section, Table } from "@/components/ui";
import { jobCards } from "@/lib/mock-data";

export default function JobCardsPage() {
  return <div className="space-y-6"><div className="flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-2xl font-semibold text-ink">Job Cards / Production</h1><p className="mt-1 text-sm text-slate-500">Track karigar work, stages, issued material, returns, wastage, loss, and labour charges.</p></div><button className="rounded-md bg-ink px-4 py-2 text-sm font-semibold text-white">Create job card</button></div><Section title="Production Job Cards"><Table headers={["Job Card", "Order", "Design", "Karigar", "Stage", "Issued", "Expected Return", "Expected Wt.", "Actual Wt.", "Wastage", "Loss", "Labour", "Status"]}>{jobCards.map((job) => <tr key={job.id}><td className="px-3 py-3 font-medium">{job.jobCardNumber}</td><td className="px-3 py-3">{job.orderNumber}</td><td className="px-3 py-3">{job.design}</td><td className="px-3 py-3">{job.karigar}</td><td className="px-3 py-3">{job.stage}</td><td className="px-3 py-3">{job.materialIssued} g</td><td className="px-3 py-3">{job.expectedReturn}</td><td className="px-3 py-3">{job.expectedWeight} g</td><td className="px-3 py-3">{job.actualReturnWeight} g</td><td className="px-3 py-3">{job.wastage} g</td><td className="px-3 py-3">{job.loss} g</td><td className="px-3 py-3"><Money value={job.labourCharges} /></td><td className="px-3 py-3"><Badge tone={job.status === "Completed" ? "green" : job.status === "Rework" ? "red" : "gold"}>{job.status}</Badge></td></tr>)}</Table></Section></div>;
}
