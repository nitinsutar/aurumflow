import Image from "next/image";
import { Badge, Section, Table } from "@/components/ui";
import { SkuGenerator } from "@/components/sku-generator";
import { variantInventory } from "@/lib/fashion-data";

export default function SkuVariantsPage() {
  return <div className="space-y-6"><div><h1 className="text-2xl font-semibold text-ink">SKU & Variant Inventory</h1><p className="mt-1 text-sm text-slate-500">Track every design by material, finish, embellishment, size, image, and stock position.</p></div><SkuGenerator /><Section title="Visual Variant Stock"><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{variantInventory.map((item) => <div key={item.sku} className="overflow-hidden rounded-lg border border-slate-200 bg-white"><div className="relative aspect-[4/3] bg-slate-100"><Image src={item.image} alt={item.designName} fill className="object-cover" sizes="(max-width: 768px) 100vw, 25vw" /><div className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-black/70 to-transparent p-3 text-white"><p className="text-sm font-semibold">{item.sku}</p><p className="text-xs">{item.manufacturer}</p></div></div><div className="space-y-3 p-4"><div><p className="font-semibold text-ink">{item.designName}</p><p className="text-sm text-slate-500">{item.baseMaterial} / {item.finish} / {item.embellishment} / {item.size}</p></div><div className="flex items-center justify-between"><Badge tone={item.stock <= item.reorderPoint ? "red" : "green"}>{item.stock} in stock</Badge><span className="text-xs text-slate-500">{item.reserved} reserved</span></div></div></div>)}</div></Section><Section title="Variant Table"><Table headers={["SKU", "Design", "Material", "Finish", "Stone", "Size", "Stock", "Reserved", "Reorder", "Velocity"]}>{variantInventory.map((item) => <tr key={item.sku}><td className="px-3 py-3 font-mono text-xs">{item.sku}</td><td className="px-3 py-3 font-medium">{item.designName}</td><td className="px-3 py-3">{item.baseMaterial}</td><td className="px-3 py-3">{item.finish}</td><td className="px-3 py-3">{item.embellishment}</td><td className="px-3 py-3">{item.size}</td><td className="px-3 py-3">{item.stock}</td><td className="px-3 py-3">{item.reserved}</td><td className="px-3 py-3">{item.reorderPoint}</td><td className="px-3 py-3">{item.velocity30d}/30d</td></tr>)}</Table></Section></div>;
}
