import { Badge, Money, Section, Table } from "@/components/ui";
import { orders } from "@/lib/mock-data";

export default function OrdersPage() {
  return <div className="space-y-6"><div className="flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-2xl font-semibold text-ink">B2B Order Management</h1><p className="mt-1 text-sm text-slate-500">Retailer orders, design approval, production status, delivery dates, and job card linkage.</p></div><button className="rounded-md bg-ink px-4 py-2 text-sm font-semibold text-white">Create order</button></div><Section title="Orders" action={<select><option>All statuses</option><option>In Production</option><option>Ready for Delivery</option></select>}><Table headers={["Order", "Customer", "Date", "Delivery", "Design", "Qty", "Metal", "Purity", "Advance", "Status", "Attachments"]}>{orders.map((order) => <tr key={order.id}><td className="px-3 py-3 font-medium">{order.orderNumber}</td><td className="px-3 py-3">{order.customer}</td><td className="px-3 py-3">{order.orderDate}</td><td className="px-3 py-3">{order.deliveryDate}</td><td className="px-3 py-3">{order.design}</td><td className="px-3 py-3">{order.quantity}</td><td className="px-3 py-3">{order.metalType}</td><td className="px-3 py-3">{order.purityKarat}K</td><td className="px-3 py-3"><Money value={order.advanceReceived} /></td><td className="px-3 py-3"><Badge tone={order.status.includes("Production") ? "gold" : order.status.includes("Ready") ? "green" : "blue"}>{order.status}</Badge></td><td className="px-3 py-3 text-slate-500">Placeholder</td></tr>)}</Table></Section></div>;
}
