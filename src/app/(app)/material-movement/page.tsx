import { PackageCheck } from "lucide-react";
import { Section } from "@/components/ui";
import { inventory, jobCards } from "@/lib/mock-data";

export default function MaterialMovementPage() {
  return <div className="space-y-6"><div><h1 className="text-2xl font-semibold text-ink">Material Issue & Return</h1><p className="mt-1 text-sm text-slate-500">Issue material to production or karigars, record return, scrap, wastage, loss, transfer, and adjustments. Every movement creates a ledger entry.</p></div><div className="grid gap-6 xl:grid-cols-2"><Section title="Issue Material"><form className="grid gap-3"><select>{inventory.map((item) => <option key={item.id}>{item.itemName} - {item.netWeight} {item.unit}</option>)}</select><select>{jobCards.map((job) => <option key={job.id}>{job.jobCardNumber} - {job.karigar}</option>)}</select><div className="grid gap-3 sm:grid-cols-2"><input placeholder="Issue weight" defaultValue="25.5" /><select><option>Issue to Karigar</option><option>Issue to Production</option><option>Transfer</option></select></div><textarea placeholder="Remarks" defaultValue="Material issue will reduce stock and create a ledger entry." /><button className="inline-flex items-center justify-center gap-2 rounded-md bg-ink px-4 py-2 text-sm font-semibold text-white"><PackageCheck className="h-4 w-4" /> Issue material</button></form></Section><Section title="Record Return"><form className="grid gap-3"><select>{jobCards.map((job) => <option key={job.id}>{job.jobCardNumber} - {job.design}</option>)}</select><div className="grid gap-3 sm:grid-cols-2"><input placeholder="Returned weight" defaultValue="24.1" /><input placeholder="Finished piece weight" defaultValue="23.4" /><input placeholder="Scrap weight" defaultValue="0.4" /><input placeholder="Loss weight" defaultValue="0.1" /></div><textarea placeholder="Remarks" defaultValue="Wastage is calculated from expected and actual weights." /><button className="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Record return</button></form></Section></div></div>;
}
